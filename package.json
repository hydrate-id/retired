{
  "name": "retired",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "bun build src/index.ts --target node --outfile api/index.js --packages external",
    "sync:domains": "bun run scripts/sync-domains.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@elysiajs/html": "^1.4.2",
    "elysia": "^1.4.0"
  }
}
